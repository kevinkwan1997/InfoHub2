import { RoundToWholePipe } from './round-to-whole.pipe';

describe('RoundToWholePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundToWholePipe();
    expect(pipe).toBeTruthy();
  });
});
